{
  "name": "relay-renderer",
  "version": "1.0.0",
  "private": true,
  "description": "Stateless render service. URL in, screenshot and DOM metrics out.",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "playwright-core": "1.62.1"
  }
}
